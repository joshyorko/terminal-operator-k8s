# --- Production Environment ---
# WARNING: THIS WILL INITIATE REAL ORDERS AND CHARGES!
# Retrieve your production token with: ssh terminal.shop -t tokens
TERMINAL_BEARER_TOKEN="YOUR_PRODUCTION_TOKEN_HERE"
TERMINAL_ENVIRONMENT="production"
# In production, use a secure mechanism to provide your card token (e.g., via K8s Secret)